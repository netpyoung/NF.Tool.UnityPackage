name: Package Tests

on: 
  push:
    branches:
      - master
  pull_request: ~

jobs:
  build:

    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.300
    - run: |
        cd src
        dotnet test
